<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
         http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <display-name>Legacy Servlet Application</display-name>
    <description>Traditional web.xml configuration demonstrating migration challenges</description>

    <!-- Context Parameters (Hardcoded Configuration Challenge) -->
    <context-param>
        <param-name>appEnvironment</param-name>
        <param-value>production</param-value>
    </context-param>
    
    <context-param>
        <param-name>maxUploadSize</param-name>
        <param-value>10485760</param-value>
    </context-param>

    <context-param>
        <param-name>apiEndpoint</param-name>
        <param-value>http://api.example.com/v1</param-value>
    </context-param>

    <!-- Session Configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <name>LEGACY_JSESSIONID</name>
            <http-only>true</http-only>
            <secure>false</secure>
        </cookie-config>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

    <!-- Lifecycle Listener (Challenge: Converting to Spring lifecycle) -->
    <listener>
        <listener-class>com.example.legacy.servlet.listener.ApplicationStartupListener</listener-class>
    </listener>

    <!-- Filters (Challenge: Converting to Spring Filter beans or Security) -->
    <!-- Security Filter - manual authentication/authorization -->
    <filter>
        <filter-name>SecurityFilter</filter-name>
        <filter-class>com.example.legacy.servlet.filter.SecurityFilter</filter-class>
        <init-param>
            <param-name>excludePatterns</param-name>
            <param-value>/public/*,/login,/css/*,/js/*</param-value>
        </init-param>
    </filter>
    
    <filter-mapping>
        <filter-name>SecurityFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Logging Filter -->
    <filter>
        <filter-name>RequestLoggingFilter</filter-name>
        <filter-class>com.example.legacy.servlet.filter.RequestLoggingFilter</filter-class>
    </filter>
    
    <filter-mapping>
        <filter-name>RequestLoggingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Character Encoding Filter -->
    <filter>
        <filter-name>CharacterEncodingFilter</filter-name>
        <filter-class>com.example.legacy.servlet.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    
    <filter-mapping>
        <filter-name>CharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Servlets (Challenge: Converting to Spring MVC Controllers) -->
    <!-- Login Servlet -->
    <servlet>
        <servlet-name>LoginServlet</servlet-name>
        <servlet-class>com.example.legacy.servlet.LoginServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    
    <servlet-mapping>
        <servlet-name>LoginServlet</servlet-name>
        <url-pattern>/login</url-pattern>
    </servlet-mapping>

    <!-- User Management Servlet -->
    <servlet>
        <servlet-name>UserServlet</servlet-name>
        <servlet-class>com.example.legacy.servlet.UserServlet</servlet-class>
    </servlet>
    
    <servlet-mapping>
        <servlet-name>UserServlet</servlet-name>
        <url-pattern>/users/*</url-pattern>
    </servlet-mapping>

    <!-- Product Catalog Servlet -->
    <servlet>
        <servlet-name>ProductServlet</servlet-name>
        <servlet-class>com.example.legacy.servlet.ProductServlet</servlet-class>
    </servlet>
    
    <servlet-mapping>
        <servlet-name>ProductServlet</servlet-name>
        <url-pattern>/products/*</url-pattern>
    </servlet-mapping>

    <!-- Data Export Servlet (demonstrates JNDI datasource usage) -->
    <servlet>
        <servlet-name>DataExportServlet</servlet-name>
        <servlet-class>com.example.legacy.servlet.DataExportServlet</servlet-class>
    </servlet>
    
    <servlet-mapping>
        <servlet-name>DataExportServlet</servlet-name>
        <url-pattern>/export/*</url-pattern>
    </servlet-mapping>

    <!-- Welcome Files -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <!-- Error Pages -->
    <error-page>
        <error-code>404</error-code>
        <location>/WEB-INF/jsp/error/404.jsp</location>
    </error-page>
    
    <error-page>
        <error-code>500</error-code>
        <location>/WEB-INF/jsp/error/500.jsp</location>
    </error-page>
    
    <error-page>
        <exception-type>java.lang.Exception</exception-type>
        <location>/WEB-INF/jsp/error/error.jsp</location>
    </error-page>

    <!-- JNDI Resource Reference (Challenge: Moving to Spring DataSource beans) -->
    <resource-ref>
        <description>Primary Database</description>
        <res-ref-name>jdbc/LegacyDB</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

    <!-- Security Constraint (Challenge: Moving to Spring Security) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Admin Area</web-resource-name>
            <url-pattern>/admin/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>admin</role-name>
        </auth-constraint>
    </security-constraint>

    <login-config>
        <auth-method>FORM</auth-method>
        <form-login-config>
            <form-login-page>/login</form-login-page>
            <form-error-page>/login?error=true</form-error-page>
        </form-login-config>
    </login-config>

    <security-role>
        <role-name>admin</role-name>
    </security-role>
    
    <security-role>
        <role-name>user</role-name>
    </security-role>

</web-app>
